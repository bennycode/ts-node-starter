pre-commit:
  parallel: true
  commands:
    eslint:
      files: git diff --name-only HEAD @{push}
      glob: '*.{.js,.jsx,.ts,.tsx}'
      run: yarn eslint {files} --fix
    prettier:
      run: yarn pretty-quick --staged --ignore-path .gitignore
