pre-commit:
  parallel: false
  commands:
    prettier:
      run: yarn pretty-quick --staged --ignore-path .gitignore
    eslint:
      glob: '*.{js,jsx,ts,tsx}'
      run: yarn eslint {staged_files} --fix
