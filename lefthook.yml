pre-commit:
  parallel: false
  commands:
    eslint:
      glob: '*.{js,jsx,ts,tsx}'
      run: yarn eslint {staged_files} --fix
    prettier:
      run: yarn prettier {staged_files} --write && exit 0
