pre-commit:
  parallel: true
  commands:
    config:
      glob: '*.{md,yaml,yml,css,scss,less,html,json}'
      run: npx prettier --write --loglevel error {staged_files}
    code:
      glob: '*.{js,jsx,ts,tsx}'
      run: npx eslint {staged_files} --fix
